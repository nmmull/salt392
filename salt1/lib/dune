(library
 (name salt1)
 (libraries menhirLib))

(ocamllex lexer)

(menhir
 (modules parser)
 (flags --table))

(rule
 (action (with-stdout-to syntaxError.ml
  (run menhir
   %{dep:parser.mly}
   --compile-errors %{dep:syntaxError.messages}))))
